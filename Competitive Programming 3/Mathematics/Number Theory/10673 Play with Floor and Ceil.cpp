#include<bits/stdc++.h>
#define forr(i,n) for(int i = 0; i < n; ++i)
#define forab(i,a,b) for(int i = a; i < b; ++i)

using namespace std;
typedef long long int lli;

int main(){
    int n;
    lli x, k;
    scanf("%d", &n);

    while(n--){
        scanf("%lld %lld", &x, &k);
        printf("0 %lld\n", k);
    }

    return 0;
}


